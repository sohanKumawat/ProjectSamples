---

 server:
     port: 8080
 spring:
     application:
              name: analytics-management-service

 eureka:
     client:
         service-url:
                  default-zone: http://localhost:8761/eureka
     instance:
       leaseRenewalIntervalInSeconds: 1
       leaseExpirationDurationInSeconds: 2
 ribbon:
  eureka:
    enabled: true
...